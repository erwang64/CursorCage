<Page x:Class="CursorCage.Views.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Accueil">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Accueil"
                   FontSize="32"
                   FontWeight="Bold"
                   Foreground="{StaticResource MainText}"
                   Margin="0,0,0,8" />

        <TextBlock x:Name="HelpText"
                   Grid.Row="1"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource SecondaryText}"
                   FontSize="15"
                   LineHeight="22"
                   MaxWidth="600"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,32"
                   Text="Limite le curseur à la fenêtre active ou à l’écran sous le pointeur, utile sur plusieurs moniteurs en jeu. Utilisez le raccourci global (Paramètres) pour verrouiller / déverrouiller sans quitter le jeu." />

        <Border Grid.Row="2"
                Padding="32"
                Background="{StaticResource BgLighter}"
                BorderBrush="{StaticResource BorderMedium}"
                BorderThickness="1"
                CornerRadius="12"
                HorizontalAlignment="Left"
                MinWidth="500">
            <StackPanel>
                <TextBlock x:Name="StatusLabel"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource MainText}"
                           Margin="0,0,0,8" />
                <TextBlock x:Name="ModeLabel"
                           Foreground="{StaticResource SecondaryText}"
                           FontSize="14"
                           Margin="0,0,0,32" />
                
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnLock"
                            Content="Verrouiller"
                            Style="{StaticResource AccentButton}"
                            Padding="24,12"
                            FontSize="15"
                            Margin="0,0,12,0"
                            Click="BtnLock_Click" />
                    <Button x:Name="BtnUnlock"
                            Content="Déverrouiller"
                            Padding="24,12"
                            FontSize="15"
                            Margin="0,0,12,0"
                            Click="BtnUnlock_Click" />
                    <Button Content="Basculer (raccourci)"
                            Padding="24,12"
                            FontSize="15"
                            Click="BtnToggle_Click" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>
